import * as nodejs from '../src/nodejs';

describe('nodejs', () => {
	describe('prepareNode()', () => {
		//
	});

	describe('downloadNode()', () => {
		//
	});

	describe('extractNode()', () => {
		//
	});

	describe('spawnNode()', () => {
		//
	});
});
